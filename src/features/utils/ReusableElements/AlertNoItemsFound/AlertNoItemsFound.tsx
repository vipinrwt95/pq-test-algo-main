import InlineAlert from '../InlineAlert/InlineAlert'

const AlertNoItemsFound = ({ message }: { message: string }) => <InlineAlert message={message} color="light dark" />

export default AlertNoItemsFound
