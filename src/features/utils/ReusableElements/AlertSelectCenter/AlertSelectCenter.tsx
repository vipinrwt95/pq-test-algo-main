import { SELECT_CENTER_FIRST } from 'src/features/constants/text'
import InlineAlert from '../InlineAlert/InlineAlert'

const AlertSelectCenter = () => <InlineAlert message={SELECT_CENTER_FIRST} />

export default AlertSelectCenter
